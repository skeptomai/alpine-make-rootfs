{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build rootfs",
            "type": "shell",
            "command": "./drive-make-rootfs.sh aarch64",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}